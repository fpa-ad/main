#include "Particle.h"

// constructor
Particle::Particle(double in_ctm, double in_x, double in_y, double in_vx, double in_vy) {
    ctm = in_ctm;
    x = in_x;
    y = in_y;
    vx = in_vx;
    vy = in_vy;
}